{
  "name": "maquina-vending-web",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/worker.js",
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "@types/node": "^26.4.0",
    "wrangler": "latest"
  }
}
